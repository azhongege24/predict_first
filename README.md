new need
1 振动分析保存结果的csv文件里，sourcefile,sourcechannel,sourcedirection里都是乱码
2 功率谱分析的最后一段，数据的时间长度不够参数设置里的分段时间长度，要去掉
3 查看保存结果打开的路径不对，应该是打开选择输出目录对应的路径
4 数据集合成时，如果没有时间列会报错。应该是没有时间特征时，就按行对齐，生成数据集
5 数据集里的特征名，带入到训练模型里了吗
6 随机搜索的内涵是什么
7调用已保存的合并数据集，有的数据集会直接退出程序，能不能提高程序容错力，或者指出数据集有什么问题，不要退出程序。暂时改不了的话，在程序带的文件里，把报错的文件都删掉